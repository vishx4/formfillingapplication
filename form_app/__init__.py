# This file makes form_app a Python package
# You can also initialize app-wide configurations or extensions here if needed.
# For now, it can be empty.

# Or, to make the app instance available when importing form_app:
# from .app import app
# This can be useful for tools like Gunicorn or when importing the app object elsewhere.
# However, for this specific setup, keeping it simple is fine.
# The app object is created and run in app.py directly.
